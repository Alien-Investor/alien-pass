app-id: org.alieninvestor.pass
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: alien-pass
separate-locales: false
# Bewusst KEIN --share=network und KEIN --filesystem: Dateien nur über das Portal (Dialog), Netz gibt es im Käfig nicht.
finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --device=dri
modules:
  - name: alien-pass
    buildsystem: simple
    build-options:   # Electron-Binaries unverändert lassen (gepinnter Hash + Fuses), kein Debug-Ableger
      no-debuginfo: true
      strip: false
    sources:
      - type: dir
        path: ../build/electron
        dest: electron
      - type: file
        path: alien-pass.sh
      - type: file
        path: org.alieninvestor.pass.desktop
      - type: file
        path: ../../icon.svg
    build-commands:
      - mkdir -p /app/main && cp -a electron/. /app/main/
      - install -Dm755 alien-pass.sh /app/bin/alien-pass
      - install -Dm644 org.alieninvestor.pass.desktop /app/share/applications/org.alieninvestor.pass.desktop
      - install -Dm644 icon.svg /app/share/icons/hicolor/scalable/apps/org.alieninvestor.pass.svg
